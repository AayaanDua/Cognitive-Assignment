my_dict = {
    "name": "Aayaan",
    "roll_no": "1024170284",
    "branch": "Computer Science and Engineering",
    "age": 19,
    "city": "Patiala",
}

# (i)
my_dict["location"] = my_dict.pop("city")

# (ii)
my_dict["cgpa"] = 8.5

# (iii)
my_dict["age"] += 1

# (iv)
copy1 = my_dict.copy()
copy2 = my_dict.copy()
removed_branch = copy1.pop("branch")
del copy2["branch"]

# (v)
for k, v in my_dict.items():
    print(k, "→", v)

# (vi)
if "email" in my_dict:
    print(my_dict["email"])
else:
    print("Email key does not exist")

# (vii)
friend_dict = {
    "name": "Rohan",
    "roll_no": "1024170999",
    "branch": "Mechanical",
    "age": 21,
    "location": "Ludhiana",
}
merged = {**my_dict, **friend_dict}
print("Merged:", merged)

# (viii)
str_dict = {k: v for k, v in my_dict.items() if type(v) is str}
print("String entries only:", str_dict)
